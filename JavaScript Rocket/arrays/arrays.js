// Manipulando Arrays

// Criar Array com construtor
let myArray = new Array ("a", "b", "c") // cria um array com 3 posições
console.log(myArray)

let myArray1 = new Array (10) // cria um array com 10 posições vazias
console.log(myArray)

// Contar elementos de um array 
console.log(["a", "b","c"].length)

// Transformar uma cadeia de caracteres em elementos de um array
let word = "hokage"
console.log(Array.from(word))

