// Operadores Logicos (logical operators)

// 2 valores booleanos, quando verificados, resultam em verdadeiro ou falso

let pao = true
let queijo = true 

// AND && 
console.log(pao && queijo) // true (as duas coisas tem que ser  verdadeiras )

// OR ||
console.log(pao || queijo) // true (uma das duas coisas é verdadeira)

// NOT !
console.log(!pao) // false
console.log(!queijo) // false
console.log(!(pao && queijo)) // false
