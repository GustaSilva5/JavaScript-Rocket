// Grouping operator ( )

let total = (2 + 3) * 5; // 5 * 5 = 25
// Quando temos um grupo de operadores, o JS entende que deve ser resolvido primeiro o que está dentro do grupo
console.log(total)